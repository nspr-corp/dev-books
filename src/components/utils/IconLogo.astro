---
import { Image } from 'astro:assets';
import logo from '../../../public/logo.png';

interface Props {
    size: string;
}

const { size } = Astro.props;
---

<Image src={logo} alt="Logo" class={`${size}`} />
